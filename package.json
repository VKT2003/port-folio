{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start:client\" \"npm run start:server\"",
    "start:client": "cd client && npm start",
    "start:server": "cd server && json-server --watch db.json --port 5000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^1.0.0-beta.1"
  }
}